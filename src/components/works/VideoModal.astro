<dialog class="video-modal" id="video-modal" aria-labelledby="video-modal-title">
  <div class="modal-shell">
    <header class="modal-header">
      <h2 id="video-modal-title"></h2>
      <button type="button" class="modal-close" aria-label="動画を閉じる" data-video-close autofocus
        >×</button
      >
    </header>
    <div class="modal-player" data-video-player></div>
    <p class="modal-help">再生できない場合はYouTubeでご覧いただけます。</p>
    <div class="modal-links">
      <a data-video-external target="_blank" rel="noreferrer">YouTubeで開く ↗</a>
      <a data-video-share target="_blank" rel="noreferrer">この作品をXで共有する ↗</a>
    </div>
  </div>
</dialog>
<script>
  import { bindVideoModal } from '../../scripts/video-modal';
  const dialog = document.querySelector<HTMLDialogElement>('#video-modal');
  if (dialog) bindVideoModal(dialog);
</script>
<style>
  .video-modal {
    width: min(calc(100% - 2rem), 64rem);
    max-height: calc(100dvh - 2rem);
    margin: auto;
    padding: 0;
    color: var(--color-text);
    border: 1px solid #a97bff80;
    border-radius: 1.25rem;
    background: #0d1220;
    box-shadow: 0 24px 100px #000a;
    overflow: auto;
    overscroll-behavior: contain;
  }
  .video-modal::backdrop {
    background: rgb(0 0 0 / 84%);
    backdrop-filter: blur(5px);
  }
  .modal-shell {
    padding: 1.25rem;
  }
  .modal-header {
    display: flex;
    gap: 1rem;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 1rem;
  }
  h2 {
    margin: 0;
    font-size: clamp(1rem, 2vw, 1.35rem);
    line-height: 1.6;
  }
  .modal-close {
    flex: 0 0 3rem;
    height: 3rem;
    border-radius: 50%;
    border: 1px solid var(--color-line);
    background: var(--color-surface);
    color: var(--color-text);
    font-size: 2rem;
    cursor: pointer;
  }
  .modal-player {
    aspect-ratio: 16/9;
    width: 100%;
    background: #000;
  }
  .modal-player :global(iframe) {
    display: block;
    width: 100%;
    height: 100%;
    border: 0;
  }
  .modal-help {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    margin-block: 1rem 0.5rem;
  }
  .modal-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 1.5rem;
  }
  .modal-links a {
    color: var(--color-cyan);
    font-size: 0.875rem;
    min-height: 2.75rem;
    display: inline-flex;
    align-items: center;
  }
</style>
